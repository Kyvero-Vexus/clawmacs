# 2026-02-26 — Daily Notes

## Summary

Major layer build day. Three layers of the clambda platform shipped:

### Layers Built

1. **cl-llm** (Layer 1) — Clean CL interface to OpenAI-compatible APIs
   - HTTP client, SSE streaming, tool call protocol
   - Tested against LM Studio at `192.168.1.189:1234`

2. **clambda-core** (Layer 2) — Agent platform
   - `agent`, `session`, `tool-registry`, `agent-turn`, `run-agent`
   - Dynamic hook vars: `*on-tool-call*`, `*on-tool-result*`, `*on-stream-delta*`
   - Built-in tools: `shell`, `read-file`, `write-file`, `list-directory`

3. **clambda-gui** (Layer 3) — McCLIM graphical frontend
   - Full McCLIM application frame
   - Chat pane with role-colored text
   - Sidebar (agent info + session stats)
   - Status bar
   - Interactor for command input
   - Commands: Send, Clear History, Switch Model, Set System, Quit
   - Streaming display via fill-pointer array accumulation
   - Tool call display via `*on-tool-call*`/`*on-tool-result*` hooks
   - Async LLM calls via bordeaux-threads

### Key Findings

- `DISPLAY=:11.0` is available on this VM — GUI works!
- McCLIM loads cleanly via Quicklisp
- Frame creates and runs; verified via `run-frame-top-level` blocking

### Mistakes Logged

- dexador `:want-stream` API
- Package export gaps (multiple)
- `return-from` inside lambdas
- `assert` syntax
- `format` tilde escaping
- McCLIM: redisplay before frame is live
- `get-output-stream-string` clears buffer
- `*on-stream-delta*` not in `clambda` top package (fixed)

### Git

All code committed and pushed to `chrysolambda/clambda` GitLab.
